services:
  socks-server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5353:5353"
    environment:
      nexus_deps_USR: testuser
      nexus_deps_psw: testpassword
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'echo > /dev/tcp/localhost/5353' 2>/dev/null || exit 1"]
      interval: 2s
      timeout: 5s
      retries: 15

  integration-tests:
    build:
      context: ./integration-tests
      dockerfile: Dockerfile
    environment:
      SOCKS_USER: testuser
      SOCKS_PASS: testpassword
    depends_on:
      socks-server:
        condition: service_healthy
